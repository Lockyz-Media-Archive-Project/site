const config = {
  siteName: "Lockyz Media Archive",
  announcementsURL: "https://cdn.lockyzdev.net/lockyz-media-archive/announcements.json",
  cdnBaseURL: "https://cdn.lockyzdev.net/",
  enableFunFacts: true,
  defaultTheme: "dark",
  maintenanceMode: false,
  // Change this at build time; do NOT expose secrets publicly.
  adminPassword: "ChangeMeAtBuildTime123"
};

export default config;
